# Data Policy

Use only synthetic data, public repositories, or explicitly approved local test data.

- `raw/`: public or synthetic raw inputs that are safe to reproduce.
- `processed/`: derived public or synthetic data.

Do not store confidential source code, credentials, private documents, model weights, large datasets, or generated training caches in this repository.

