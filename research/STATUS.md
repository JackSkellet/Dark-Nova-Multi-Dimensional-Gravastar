# Status

Current phase: Phase 0 bootstrap plus reduced Phase 5 component prototypes complete.

## Completed

- Confirmed repository was blank except `.git`.
- Recorded environment summary.
- Created Python project, tests, reusable prototype package, CLI experiment runner, and research document structure.
- Formalized H1-H5 and null hypotheses.
- Expanded primary-source review and pinned DS4 inspection to commit `80ebbc396aee40eedc1d829222f3362d10fa4c6c`; `research/18_ds4_source_audit.md` records local source evidence for imatrix collection, tensor-family quantization policy, weighted quantization, quality testing, routed kernels, and SSD expert streaming.
- Ran E1-E5 reduced experiments with seed 123, plus E1b routing robustness, E3b/E3c/E3d/E3e/E3f/E3g precision follow-ups, E3h real open-model matrix precision, E3i real open-model token-id task-loss precision, E3j real open-model natural-text embedding precision, E3k real open-model natural-text internal-tensor precision, and E4b routed-execution lookup follow-up.
- Added and ran E4c Torch-backed batched routed-execution benchmark; `configs/smoke.yaml` now requests ROCm through PyTorch HIP, and the project runtime now uses ROCm PyTorch.
- Added and ran E4d ROCm transfer-scaling benchmark for separate host-to-device, simple device dispatch, and device-to-host timing across payload sizes.
- Added and ran E5c trainable low-rank adapter-versus-retrieval chronological experiment.
- Added and ran E5d synthetic stale-doc positive-control chronology with an API rename and later documentation fix.
- Added and ran E5e synthetic patch-generation functional positive control with candidate diffs graded by unit tests.
- Added and ran E5f public generated patch candidate on `pallets/markupsafe` commit `54bb00b`, graded by an executable proxy-object regression command and compared with historical replay.
- Added deterministic generated patch heuristics for one-sided Python `clamp(value, low, high)` implementations and Kilo-style `saved_hl` restore leaks; the clamp heuristic is covered by executable synthetic public-history replay tests, and the `saved_hl` heuristic is covered by a real public Kilo source-level regression.
- Added and ran E5g public patch replay suite across MarkupSafe `54bb00b` and Kilo `8e9a9bb`, preserving task-level generated, baseline, and historical-patch outcomes.
- Ran explicit public-repository changed-file, symbol-QA, and stale-document chronology attempts on `antirez/kilo` and `pallets/markupsafe`.
- Ran S1 synthetic security canary/access-control experiment.
- Added and ran E5b external update-controller simulation for versioned adapter candidates, regression/security gates, tamper detection, audit logging, and rollback.
- Added and ran S2 synthetic adversarial security suite for bypass prompts, canary extraction, prompt-injection variants, poisoning/backdoor text, poisoned update rejection, tamper detection, and rollback.
- Added and ran S2b obfuscated extraction corpus for separator-obfuscated canaries, base64/hex canaries, paraphrased stored-instruction text, poisoning text, and repeated bypass queries.
- Added and ran S2c semantic extraction red-team smoke test for sensitive aliases, trust-label quarantine, semantic exfiltration query denial, and clean-query preservation.
- Added and ran E6a structured external repository-memory experiment as an alternative architecture family outside the original five ideas.
- Added and ran E6b public repository symbol-memory QA on local public `kilo` and `markupsafe` clones.
- Added and ran E6c public repository signature-memory QA on local public `kilo` and `markupsafe` clones.
- Added and ran E6d public repository call-stub generation proxy on local public `markupsafe`.
- Added and ran E6e public repository function-skeleton generation proxy on local public `markupsafe`.
- Added and ran E6f public repository docstring-skeleton generation proxy on local public `markupsafe`.
- Added and ran E6g public repository API-reference generation proxy on local public `markupsafe`.
- Added and ran E6h public repository API-doc coverage QA on local public `markupsafe`.
- Added and ran E6i synthetic API-doc drift detection positive control for source/docs inconsistency.
- Added `research/19_real_training_transition_audit.md` to separate truly trained work from simulations before useful-scale training starts.
- Added and ran `R1_rocm_training_validation`: ROCm PyTorch reports HIP 7.2.53211 on an AMD Radeon RX 9070 XT, validates FP32/BF16/FP16 forward-backward smoke paths, reaches stable tiny-decoder batch 64 / 8192 tokens per step, and confirms checkpoint resume.
- Added and ran `D1_corpus_preparation`: built the first licensed local corpus manifest from `kilo`, `markupsafe`, and `ds4`; the corpus is real but insufficient at 428861 approximate tokens against the 50M-token requirement.
- Added and ran dense decoder smoke training records: ROCm FP32 fails with nonfinite loss, ROCm BF16 triggered a GPU hang, and the same pipeline completes on CPU with checkpoint resume.
- Added `research/20_rocm_rdna4_vllm_notes.md` after checking the user-supplied RDNA4/gfx1201 vLLM thread and linked upstream PRs; it is external serving-stack context, not direct PyTorch training evidence.
- Added and ran `R2_rocm_transformer_stability`: staged ROCm component microbench for embedding, normalization, MLP, attention, mask mode, FP32/FP16/BF16, optimizer step, and size thresholds.
- Updated dense training to use additive causal masks by default after R2 showed bool causal mask failures on ROCm; added explicit optimizer selection.
- Added `T1c_rocm_dense_decoder_additive_mask_smoke`, the first completed ROCm dense decoder smoke after the mask fix.
- Added 12.9M-parameter ROCm dense probes `T2`, `T2a`, `T2b`, and `T2c`; all preserve failure evidence for step-2 nonfinite loss at the 10M+ scale.
- Added dense step-stability debug probes `T3` through `T3e` to locate the first nonfinite phase and compare layer/width thresholds.
- Added `T4_rocm_dense_decoder_11m_sgd_200step`, the first completed 10M+ parameter ROCm dense training probe from random initialization.
- Added `D2_expanded_corpus_preparation` from additional local permissive public repositories; it reached 27,380,075 approximate tokens but is transitional evidence because the active goal now requires a Hugging Face `datasets` corpus.
- Added `T5_rocm_dense_decoder_11m_sgd_1m_tokens`, a 4,000-step / 1,024,000-token ROCm run on the stable 11M dense lane with checkpoint resume.
- Added `D3_hf_corpus_manifest`, the first exploratory-research-only Hugging Face corpus manifest with pinned revisions, accessible Dataset Viewer parquet metadata, mixed-license metadata recording, and a no-remote-dataset-script `datasets` streaming policy.
- Added `D4_hf_corpus_materialization`, a filtered local exploratory HF mirror with more than 50M train tokens, repository-aware splits, checksums, and technical filtering.
- Added periodic dense-training progress/checkpoint flushing after a killed silent 50M-token run left no useful partial evidence.
- Added `T6a_rocm_dense_decoder_11m_hf_d4_progress_probe`, an instrumented 2.56M-token ROCm dense probe on the HF mirror showing checkpoint flushes and downward loss movement.
- Added a limitation-aware research assessment layer that classifies current negative, mixed, smoke-pass, and narrow-positive results without converting green experiment runs into unsupported success claims.
- Updated `scripts/run_experiments.py` so the core runner preserves existing standalone result records in `results/manifest.json` instead of dropping prior explicit experiments.

## Active Hypotheses

H1-H5 and A1 structured external memory are active. E6 integrated design is deferred.

## Experiments Run

Tests: `uv run pytest` passed with 77 tests after E6i was added, and `uv run ruff check .` passed. Additional invariants passed for the 44-record manifest, E6i assessment, and ROCm runtime smoke. Git: the reduced-research snapshot was committed as `28bab1911bd991db920ab7b6906bbec8e81c458d` and tagged `reduced-research-baseline-2026-06-20`; all 44 experiment result files were then regenerated from that committed tree so no result file reports `git_commit: uncommitted`. A second transition commit `9ae7a7b1f14e930195c0eceea225a537b70aa071` was tagged `real-training-transition-2026-06-20`. Experiments: `uv run python scripts/run_experiments.py --seed 123 --config configs/smoke.yaml` completed and wrote ROCm-backed E4c/E4d metrics plus E6a-E6i structured-memory metrics. Public chronology commands for `kilo` and `markupsafe` completed earlier and their standalone result files are preserved in the manifest. Real-training bootstrap commands wrote `R1_rocm_training_validation`, `D1_corpus_preparation`, `T1_dense_decoder_training_smoke`, `T1a_rocm_dense_decoder_bf16_training_failure`, `T1b_cpu_dense_decoder_training_smoke`, `R2_rocm_transformer_stability`, `T1c_rocm_dense_decoder_additive_mask_smoke`, `T2_rocm_dense_decoder_10m_additive_probe`, `T2a_rocm_dense_decoder_10m_additive_lr1e6_probe`, `T2b_rocm_dense_decoder_10m_additive_sgd_probe`, `T2c_rocm_dense_decoder_10m_additive_sgd_lr0_probe`, `T3_dense_step_stability_10m_sgd_lr0`, `T3a_rocm_dense_step_stability_512x1_sgd_lr0`, `T3b_rocm_dense_step_stability_512x2_sgd_lr0`, `T3c_dense_step_stability_512x3_sgd_lr0`, `T3d_dense_step_stability_256x4_sgd_lr0`, `T3e_dense_step_stability_544x3_sgd_lr0`, and `T4_rocm_dense_decoder_11m_sgd_200step`. Follow-up commands wrote `D2_expanded_corpus_preparation`, `T5_rocm_dense_decoder_11m_sgd_1m_tokens`, `D3_hf_corpus_manifest`, `D4_hf_corpus_materialization`, and `T6a_rocm_dense_decoder_11m_hf_d4_progress_probe`. `uv run python scripts/summarize_research_assessment.py` wrote `results/research_assessment.json` from the current 67-record manifest.

## Important Results

- E1: hierarchy preserved synthetic route accuracy and used fewer toy active components than flat contextual routing, but had higher p95 routing latency.
- E1b: single-label contextual routing had mixed-route recall 0.500. Naive multi-label routing reached route recall 1.000 but added 0.258 false positive routes/sample and adversarial exact accuracy was 0.735. Latent-centroid multi-label routing reached exact set accuracy 1.000 but p95 latency rose to 2.779 ms.
- E2: uniform int8 dominated tested compositional methods on the synthetic matrix, including explicit Kronecker rank-1 and Tensor Train 4D baselines.
- E2 product quantization: product-quantized rows used 4288 bytes with MSE 30.298383 versus uniform int8 at 4108 bytes with MSE 0.004992, so the codebook baseline was rejected after overhead accounting.
- E2 tensor baselines: Kronecker rank-1 used 544 bytes but had MSE 59.978289; Tensor Train 4D used 4704 bytes with MSE 30.645373, losing to uniform int8 on both bytes and error.
- E3: causal ranking found planted carriers, but causal FP32 protection did not beat random protection on output MSE.
- E3b: groupwise int4 reduced MSE versus global int4, and output-error-selected FP32 protection beat random protected controls at the same 144-byte budget on a synthetic validation set.
- E3c: in a random tiny transformer-style LM with held-out prompts, output-error-selected FP32 output-head rows reduced held-out logit MSE to 0.007662 at 1368 bytes versus random mean 0.015225 and groupwise int4 0.018951.
- E3d: in a ridge-trained tiny transformer-style LM, trained held-out NLL improved from 4.417 to 1.920 versus untrained. Output-error-selected FP32 rows had held-out logit MSE 0.003384 at 1368 bytes versus random mean 0.004833, but the best random NLL draw was slightly better than selected.
- E3e: selected BF16-like carrier rows had held-out logit MSE 0.003354 at 1080 bytes versus random BF16 mean 0.005185. Selected FP32 carrier rows had 0.003353 at 1368 bytes, so FP32 gave negligible extra logit-MSE benefit over BF16-like carriers in this toy output-head run.
- E3f: internal MLP output rows selected by train-prompt output error had held-out logit MSE 0.001214 at 1952 bytes versus random FP32 protected mean 0.001845 and groupwise int4 0.002137. NLL remained mixed: selected NLL was 1.915895 while groupwise int4 and random protected controls were lower in this seed.
- E3g: after training the internal `w_mlp_out` matrix itself, selected BF16-like protected rows had held-out logit MSE 0.450158 at 1568 bytes versus random BF16 mean 0.644747 and groupwise int4 0.767562. Selected FP32 rows had similar logit MSE 0.450777 at 1952 bytes, so FP32 did not improve the logit-MSE tradeoff over BF16-like rows in this seed.
- E3h: on pinned public `sshleifer/tiny-gpt2` tensor `transformer.wte.weight` with shape `[50257, 2]`, selected BF16/FP32 protected rows beat matched random controls on reconstruction MSE, but row-wise groupwise metadata dominated storage. Groupwise int4 used 653341 bytes and selected BF16 used 654365 bytes versus full FP32 at 402056 bytes; uniform int4 used 50269 bytes but had higher reconstruction MSE. This is real-checkpoint evidence against a naive protected-row layout for skinny matrices.
- E3i: on the same pinned tiny-gpt2 tensor with a local minimal GPT-2 forward pass, selected BF16/FP32 protected rows did not beat matched random protected-row means on held-out KL. Selected BF16 KL was 0.000000885 versus random BF16 mean 0.000000842; selected FP32 KL was 0.000000860 versus random FP32 mean 0.000000839. This is real-checkpoint task-loss evidence against the current calibration-row selector for this tensor/workload.
- E3j: on byte-level GPT-2 BPE tokenized natural-language repository/code prose, selected BF16 protected rows had held-out KL 0.000000178 versus random BF16 mean 0.000000580, and selected FP32 protected rows had held-out KL 0.000000214 versus random FP32 mean 0.000000581. They did not beat the best random draws and still used 653597/653725 bytes versus full FP32 at 402056 bytes and uniform int4 at 50269 bytes, so this improves task realism but still does not show a storage Pareto win.
- E3k: on internal tensor `transformer.h.0.mlp.c_fc.weight` with shape `[2, 8]`, all precision policies had held-out KL 0.000000000 after numerical roundoff clamping. Uniform int4 used 20 bytes, groupwise int4 used 32 bytes, selected BF16 used 52 bytes, selected FP32 used 68 bytes, and full FP32 used 64 bytes. This fixes the skinny-embedding storage confound but finds no task-loss signal for selective precision on the tiny internal tensor.
- E4: dense/exact lookup remained strongest at useful recall for tested bank sizes.
- E4b: with full-path simulation, exact indexing beat dense routing at larger banks and cached exact routing improved repeated-query end-to-end latency; hash indexing saved memory but had poor recall and worse latency.
- E4c: Torch batched exact routing requested `device=rocm` and the current project PyTorch runtime reports `torch 2.12.1+rocm7.2`, HIP `7.2.53211`, `rocm_available=1`, `accelerator_backend=rocm`, and `uses_rocm_transfer=1`. PyTorch exposes the ROCm execution device as type `cuda`. At bank 512, p50 end-to-end was 0.5172 ms with recall 1.000 and p95 had a 30.1241 ms outlier; at bank 2048, p50 was 0.3888 ms and p95 was 0.6132 ms. This proves the ROCm path runs, but it is still a small benchmark rather than a GPU speed claim.
- E4d: ROCm transfer scaling recorded host-to-device, simple device dispatch, and device-to-host timing for 1, 8, and 32 MiB payloads. Best p50 host-to-device bandwidth was 22.917 GB/s at 8 MiB; best p50 device-to-host bandwidth was 28.715 GB/s at 8 MiB. This is a useful ROCm transfer baseline, but it does not measure occupancy, fusion, power, decompression kernels, or useful model layers.
- R1: ROCm training validation reports `torch 2.12.1+rocm7.2`, HIP `7.2.53211`, `accelerator_backend=rocm`, device type `cuda`, GPU `AMD Radeon RX 9070 XT`, 17095983104 total bytes VRAM, and memory snapshot free 15680405504 / allocated 159383552 / reserved 801112064 bytes. FP32, BF16, and FP16 forward-backward smokes passed. Tiny-decoder batch sweep was stable through batch 64 / 8192 tokens per step, and checkpoint resume passed. This is training-readiness evidence, not useful-scale training evidence.
- External RDNA4/vLLM check: the user-supplied Reddit thread describes 2x Radeon AI PRO R9700 / gfx1201 on ROCm 7.2 and vLLM 0.22.1. The reported useful fixes are AITER Unified Attention for the long-context decode cliff, bf16/fp16 KV rather than FP8 KV, and a non-CUDA Gated-DeltaNet KKT operand-layout fix for TP>=2 startup hangs. The linked PRs are open upstream as `vllm-project/vllm#46190` and `vllm-project/vllm#46192`. This is serving-stack evidence, not direct PyTorch training evidence, but it reinforces the decision to avoid bool-mask/fused-kernel assumptions and isolate ROCm transformer paths before scaling.
- R2: staged ROCm transformer microbench records 17 component cases. Embedding forward/loss/backward/optimizer, normalization, MLP, unmasked attention, default bool causal attention, additive causal attention, FP16/BF16 transformer, and larger 8x128 hidden-128 transformer cases passed. Two cases failed with nonfinite loss: FP32 attention with bool causal mask forced through math SDP, and FP32 transformer with bool causal mask. This points to mask/kernel-path sensitivity rather than a universal transformer failure.
- E5: retrieval and replay simulation retained facts and rollback worked; retrieval baseline was already strong.
- E5c: trainable low-rank adapter with replay reached 1.000 final accuracy and 1.000 prior accuracy, while no-replay adapter fell to 0.500 final accuracy and 0.400 prior accuracy. Updated retrieval also reached 1.000 accuracy with 235 bytes versus 384 adapter bytes, so retrieval was not beaten.
- E5d: synthetic stale-doc positive control detected 1 expected stale-doc issue after `parse_config` was renamed to `load_config`, reached retrieval and structured top-k accuracy 1.000, and confirmed no stale-doc files after a later documentation fix.
- E5e: synthetic patch-generation control graded three candidate diffs by running unit tests. No-op and wrong one-sided patch failed; structured patch passed; functional success rate was 0.333.
- E5f: public MarkupSafe commit `54bb00b` was evaluated from parent `b5291646cbab`. The proxy-object regression failed before the patch and for no-op, while a deterministic generated `s.__class__ is str` to `type(s) is str` candidate and the historical future diff both passed; functional success rate was 0.667. The replay harness now also includes a generated clamp-bounds heuristic, but the MarkupSafe state did not contain that pattern. This remains narrow hand-written heuristic evidence rather than broad autonomous patch generation.
- E5g: two public historical patch replay tasks were evaluated: MarkupSafe `54bb00b` with a functional proxy-object regression and Kilo `8e9a9bb` with an executable source-level `saved_hl` regression. Both baselines failed, both historical patches passed, and generated deterministic candidates passed both task regressions; generated task success rate was 1.000 across 2 tasks. This is broader than E5f but still narrow hand-written heuristic evidence.
- E5 public chronology: on 7 non-empty future changed-file steps from `antirez/kilo`, frozen top-k accuracy was 0.714, updated retrieval was 0.857, and structured memory was 1.000. Evidence is weak because the repo is tiny and most targets are one file.
- E5 public symbol QA: final prior symbol top-k accuracy was 1.000 for frozen, retrieval, and structured memory; only one future step introduced a new symbol. This satisfies a first repository-QA attempt but is weak because the project is tiny.
- E5 markupsafe symbol QA: on 30 commits, final structured prior symbol top-k accuracy was 1.000 versus retrieval 0.165 and frozen 0.924.
- E5 stale-doc detection: synthetic local test detects stale docs after symbol removal. Public scans found no stale-doc issue in sampled `kilo` or `markupsafe` windows, so public stale-doc quality remains unmeasured.
- S1 security: unauthorized retrievals 0, canary leaks 0, blocked synthetic secret docs 1, quarantined prompt-injection docs 1, audit events 4, critical failures 0.
- E5b update controller: one clean externally approved adapter candidate accepted, three unsafe candidates rejected, tamper detection worked, one canary-leaking update was rejected, rollback to base succeeded, and no critical security failure was deployed.
- S2 adversarial security: access-control bypass successes 0, canary extraction successes 0, prompt-injection bypass successes 0, poisoned update acceptances 0, tampered update acceptances 0, one poisoned update candidate rejected, and critical failures 0.
- S2b obfuscated extraction corpus: 12 queries and 12 documents produced 0 unauthorized retrievals, 0 canary leaks, 4/4 encoded or separator-obfuscated canary documents quarantined, 4/4 paraphrased stored-instruction documents quarantined, 1 poisoned document quarantined, 144 audit events, and 0 critical failures.
- S2c semantic extraction red-team smoke test: 8 queries and 6 documents produced 0 unauthorized retrievals, 0 sensitive-alias leaks, 3/3 semantic secret documents quarantined by trust label, 6/6 semantic exfiltration queries denied before retrieval, 2/2 clean queries allowed, 18 audit events, and 0 critical failures.
- E6a structured external repository memory: on a synthetic repository-convention task, gated structured external memory reached answer accuracy 1.000, restricted-denial accuracy 1.000, authorized-security accuracy 1.000, and 0 poisoned answers. Ungated updated text retrieval reached answer accuracy 0.600 and produced 1 poisoned answer. This is an exploration-positive external-memory signal, but the parametric baseline is a proxy and the task is not code generation.
- E6b public repository symbol-memory QA: on local public `kilo` commit `323d93b29bd8` and `markupsafe` commit `b2e4d9c7687b`, structured symbol memory answered 70/70 symbol-file questions correctly, while text file retrieval answered 12/70 correctly. Structured storage was 3641 bytes versus 1229 bytes for the tokenized text-retrieval proxy. This is real public-source QA evidence for symbol-file lookup only, not code generation or natural-language reasoning.
- E6c public repository signature-memory QA: on the same local public clones, structured signature memory answered 23/23 defining signature-line questions correctly, while text signature lookup answered 22/23 correctly. Structured storage was 1469 bytes versus 49153 bytes for the source-line lookup proxy. This is real public-source QA evidence for signature-line lookup only, not code generation or semantic code understanding.
- E6d public repository call-stub generation: on local public `markupsafe` commit `b2e4d9c7687b`, structured signature memory generated 64/64 canonical Python call stubs correctly after annotated signatures were included, while a name-only call-stub baseline generated 26/64 correctly. This is a constrained code-generation proxy from extracted signatures only, not executable argument synthesis or semantic program generation.
- E6e public repository function-skeleton generation: on local public `markupsafe` commit `b2e4d9c7687b`, structured signature memory generated 64/64 minimal Python function skeletons correctly after annotated signatures were included, while a name-only skeleton baseline generated 0/64 correctly. This is a constrained code-generation proxy from extracted signatures with a deterministic placeholder body, not executable behavior synthesis or semantic implementation generation.
- E6f public repository docstring-skeleton generation: on local public `markupsafe` commit `b2e4d9c7687b`, structured docstring memory generated 10/10 minimal Python function skeletons with first docstring lines correctly, while a signature-only skeleton baseline generated 0/10 correctly. This is a constrained code-and-documentation proxy from extracted signatures and first docstring lines only, not executable behavior synthesis or semantic implementation generation.
- E6g public repository API-reference generation: on local public `markupsafe` commit `b2e4d9c7687b`, structured docstring memory generated 10/10 Markdown API reference entries with file path, signature, and first docstring line correctly, while a signature-only API-reference baseline generated 0/10 correctly. This is a constrained documentation-generation proxy from extracted signatures, source paths, and first docstring lines only, not semantic documentation generation or quality-rated documentation.
- E6h public repository API-doc coverage QA: on local public `markupsafe` commit `b2e4d9c7687b`, structured documentation-directive memory answered 4/4 API documentation coverage questions correctly for `docs/escaping.rst`, while a source-symbol-only memory baseline answered 0/4 correctly. This is exact Sphinx directive coverage lookup only, not documentation quality evaluation or semantic drift detection.
- E6i synthetic API-doc drift detection: a generated local Git history renamed `parse_config` to `load_config` in source while `docs/api.rst` still referenced `.. autofunction:: parse_config`. Structured source/documentation consistency memory detected the stale missing-source directive 1/1, while the doc-directive-only baseline answered 0/1 correctly. This is a synthetic positive control for directive/source inconsistency only, not public evidence or semantic prose drift detection.
- D1: corpus preparation produced 100 documents from 3 permissively licensed local repositories, 428861 approximate tokens, licenses BSD/MIT, and language coverage across C, headers, Python, Markdown, reStructuredText, JSON, YAML, TOML, and text. It reports `meets_50m_token_requirement=false`, so it is a pipeline proof and not yet a sufficient training corpus.
- T1/T1a/T1b/T1c: the dense decoder training pipeline exists with byte-level tokenization, configurable small Transformer, additive causal mask mode, explicit optimizer selection, checkpoint resume, validation loss, generation samples, token accounting, and machine-readable metrics. Original ROCm FP32 failed with bool causal masking, ROCm BF16 failed harder with a GPU hang, CPU FP32 completed, and additive-mask ROCm FP32 completed 5 steps with validation loss 5.645702838897705 and checkpoint resume. This is the first completed ROCm dense smoke, but still below useful model scale.
- T2/T2a/T2b/T2c: 12.9M-parameter additive-mask ROCm dense probes reached the 10M parameter floor but all failed with nonfinite loss at step 2. The failure persists with AdamW at lr 5e-5, AdamW at lr 1e-6, SGD at lr 1e-4, and SGD at lr 0. This shows the next blocker is not simply learning-rate magnitude or AdamW; it is a second-iteration state/scale issue in the 10M path.
- T3/T3a/T3b/T3c/T3d/T3e: dense step-debug probes show the 12.9M 512-hidden 4-layer path has finite initial parameters, finite first logits, and finite first loss, but nonfinite gradients already appear after the first backward. The first nonfinite tensor is `blocks.layers.1.norm2.weight`, and 1592 gradient values are nonfinite. 512-hidden with 1/2/3 layers stays finite, 256-hidden with 4 layers stays finite, and a 544-hidden 3-layer shape reaches 11025505 parameters while staying finite for the two-step debug window.
- T4: `T4_rocm_dense_decoder_11m_sgd_200step` trained an 11,025,505-parameter dense decoder from random initialization for 200 ROCm steps on the prepared real corpus. It consumed 51200 training tokens, completed with validation loss 5.783808588981628, checkpoint resume passed, and loss moved from 5.860136032104492 at step 1 to 5.769588470458984 at step 200. This satisfies the first 10M+ dense-training foothold, but it is still far short of the required 50M-token run and has no functional coding evaluation yet.
- D2: `D2_expanded_corpus_preparation` accepted 17 local permissive public repositories, 9,043 documents, and 27,380,075 approximate tokens. It still reports `meets_50m_token_requirement=false`; because the active goal now requires Hugging Face `datasets`, this is preserved as transitional local-corpus evidence rather than the target corpus.
- T5: `T5_rocm_dense_decoder_11m_sgd_1m_tokens` trained the stable 544-hidden / 3-layer / 8-head / additive-mask / SGD / FP32 dense lane for 4,000 ROCm steps and 1,024,000 train tokens. It completed in 38.31 seconds at about 26,730 tokens/s, with validation loss 5.674013078212738 and checkpoint resume passing. This is the strongest dense-baseline runtime result so far, but it is still below the 50M-token requirement and was trained on the transitional local corpus.
- D3: `D3_hf_corpus_manifest` selected two HF datasets and seven accessible exploratory configs: `CodedotAI/code_clippy_github` at revision `cf9f33dec640f45228f8f3f5e6a7899a37f5f83e` with `JavaScript-all`, `all-mit`, and `all-apache-2.0`; plus `codeparrot/github-code-clean` at revision `c48d40f9e70f0196f8236901ee35807f7d6c44c0` with `Python-all`, `JavaScript-all`, `all-mit`, and `all-apache-2.0`. The manifest records 3,544,000 rows and 12,399,825,013 parquet bytes, labels the corpus `exploratory-research-only`, records mixed/unclear license metadata instead of rejecting accessible data, and specifies `datasets.load_dataset("parquet", ...)` streaming from Dataset Viewer parquet URLs without executing dataset repository scripts.
- HF `datasets` smoke: direct `load_dataset("CodedotAI/code_clippy_github", ...)` failed because current `datasets` rejects the repository's legacy dataset script, which is compatible with the no-remote-script policy. Streaming one Dataset Viewer parquet URL through `load_dataset("parquet", ...)` read a real row with columns `code_text`, `file_path`, `language`, `license`, `repo_name`, and `size`; the process then hit a PyArrow/Python finalization crash after output, so the next materializer should run row streaming in short subprocess chunks or otherwise handle cleanup carefully.
- D4: `D4_hf_corpus_materialization` streamed Dataset Viewer parquet URLs through `datasets` in a child process to avoid the local PyArrow finalization abort. It wrote `data/hf_mirror/exploratory_d3/corpus.jsonl` with SHA256 `547b2ff1d9eedd61f4791d6cffb256ec844e8312513ec529b953952511f3a2bc`, 3,415 accepted rows, 50,102,528 train tokens, 1,455,924 validation tokens, 735,566 test tokens, and `meets_50m_token_requirement=true`. It rejected 2,393 rows: 1,985 vendor/generated paths, 181 exact duplicates, 119 too-large rows, 58 secret/confidential patterns, 45 generated markers, and 5 evaluation-contamination paths. The first materialized 50M-token mirror came entirely from `CodedotAI/code_clippy_github::JavaScript-all` because that config alone crossed the token target.
- Killed T6 attempt: the first full `T6_rocm_dense_decoder_11m_hf_d4_50m_tokens` run was manually killed after about 10 minutes at the user's request. It left only an empty artifact directory and no result JSON/checkpoint, proving the previous end-only checkpoint behavior was not acceptable for long runs.
- T6a: `T6a_rocm_dense_decoder_11m_hf_d4_progress_probe` trained the stable 11M dense lane on the D4 HF mirror for 10,000 ROCm steps / 2,560,000 tokens. It flushed progress every 500 steps and `dense_decoder_latest.pt` every 1,000 steps. Throughput was 26,818.91 tokens/s, checkpoint resume passed, validation loss was 5.621392607688904, first step loss was 5.706747055053711, final step loss was 5.408170700073242, and the best interval loss was 5.239053249359131 at step 9,000. This shows the HF dense run is actually learning, but it is still below the required 50M-token training run.
- Assessment summary: `results/research_assessment.json` contains 67 records and still reports `pareto_improvement_found=false`. It classifies R1 as training-readiness-positive, R2 as transformer-stability-failed, D1/D2 as corpus-prepared-insufficient-tokens, D3 as HF-corpus-manifest-positive, D4 as HF-corpus-materialized, T3/T2-class failures as preserved negative evidence, T3e/T4/T5/T6a as positive stability/training probes, and the earlier E6 structured-memory records as proxy or measurement positives rather than architecture proof.

## Failed Experiments

The initial intentional RED test run failed with `ModuleNotFoundError: No module named 'weightlab'` before implementation. The S2c RED test failed with `ImportError: cannot import name 'run_semantic_extraction_red_team_experiment'` before the semantic extraction smoke test was implemented. The assessment RED test failed with `ModuleNotFoundError: No module named 'weightlab.assessment'` before the assessment module was implemented, then exposed a manifest-shape mismatch before the loader was corrected. The E6a external-memory RED test initially failed because unauthorized security-scope questions returned unknown instead of deny; the structured-memory path now detects trusted but unauthorized matching records before answering. The E6b assessment RED test initially failed because `results/E6b_public_repository_memory_qa.json` did not exist before the new experiment was wired into the runner and regenerated. The E6c signature-QA tests first failed on missing import, then on missing behavior, before signature extraction and result generation were implemented. The E6d call-stub tests first failed on missing import, then on missing behavior, before call-stub generation and result generation were implemented. The E6e function-skeleton tests first failed on missing import, and the assessment test failed before the new result was wired into the runner and regenerated. The E6f docstring-skeleton test first failed on missing import, then the annotated-signature test failed because documented annotated functions were skipped; widening Python signature extraction fixed the public MarkupSafe docstring benchmark. The E6g API-reference generation test first failed on missing import, and the assessment test failed before the new result was wired into the runner and regenerated. The E6h API-doc coverage test first failed on missing import, and the assessment test failed before the new result was wired into the runner and regenerated. The E6i API-doc drift detection test first failed on missing import, and the assessment test failed before the new result was wired into the runner and regenerated. The first ROCm dense training smoke failed with nonfinite loss, and the larger BF16 ROCm dense smoke failed with a GPU hang. R2 also preserves two mask-sensitive nonfinite failures, T2/T2a/T2b/T2c preserve 10M-scale step-2 nonfinite failures, and T3 preserves the first observed nonfinite phase as step-1 LayerNorm gradients in the 512x4 path.

## Blockers

The immediate blocker is restarting the stable 11M dense lane for the full 50M-token D4 corpus run using the new periodic checkpoint/progress flushing, then training a matched candidate. The 512x4 ROCm path is rejected for now because it creates nonfinite LayerNorm gradients on the first backward; the stable dense baseline lane is 544-hidden, 3 layers, additive causal mask, SGD. Larger model experiments and richer public-repository QA/code-generation experiments remain unimplemented.

## Resource Usage

Downloaded one public tiny open-model checkpoint and tokenizer: pinned `sshleifer/tiny-gpt2` `pytorch_model.bin`, `vocab.json`, and `merges.txt` at commit `5f91d94bd9cd7190a9f3216ff93cd1dd95f2c7be`, about 3.7 MiB under ignored `data/raw/public/tiny-gpt2`. No private weights or gated datasets downloaded. The project `uv` environment now installs PyTorch from the ROCm 7.2 wheel index plus `triton-rocm`, and includes `datasets`, `pyarrow`, and `pandas` for HF parquet streaming. The exploratory HF mirror is ignored under `data/hf_mirror/exploratory_d3/corpus.jsonl`, 58,615,129 bytes. T6a artifacts are ignored under `artifacts/dense_decoder_11m_hf_d4_progress_probe`, including a 44,119,189-byte latest checkpoint and a final checkpoint. Generated `results/` now includes 67 manifest records.

## Decisions

- Start with small falsifying experiments.
- Defer integrated architecture.
- Treat DS4 as a selective-precision and routed-expert execution source case study, not evidence of semantic carrier pathways or a reproduced GPU Pareto result.
- The next phase must prioritize real local training and evidence-based architecture selection; the current 67-record snapshot is a reduced-research baseline plus real-training bootstrap evidence, not a completed architecture result.
- Hugging Face corpus selection is exploratory-research-only. Mixed or unclear license metadata is recorded and does not block exploratory training, but the corpus is not approved for production, redistribution, or company deployment.

## Next Highest-Value Action

Restart the stable 11M dense lane on the D4 mirror for the full 50M-token baseline using `--progress-interval` and `--checkpoint-interval`, then build the matched candidate on the same tokenizer, data, and token budget.
